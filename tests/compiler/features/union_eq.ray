union Foo { a, b }
union Bar {
    a: int,
    b: str,
}

fn main() {
    foo: Foo = .a
    if foo == .a do print "I'm foo.a"
    else if foo == .b do print "I'm foo.b"

    bar: Bar = .b("ee")
    if bar == .a do print "I'm bar.a"
    else if bar == .b do print "I'm bar.b"
}

