use data.math{Vec2, introduce}
use data.geom{ Point, Square }

fn main() {
    v := Vec2{ x=3, y=9 }
    pt := Point{ x=5, y=8 }

    {
        use data.math{Vec2 as Pipou,}
        v := Pipou{ x=55, y=333 }
    }

    introduce()
}

