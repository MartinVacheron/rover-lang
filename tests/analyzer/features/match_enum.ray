enum Marker {
    cross,
    circle,
    none
}

enum Bar{a}

fn main() {
    marker: Marker = .none
    bar: Bar = .a

    match marker {
        Marker.circle => print "circle"
        Marker.cross => print "cross"
        Marker.none => print "none"
    }

    match marker {
        .circle => print "circle"
        .cross => print "cross"
        .none => print "none"
    }

    foo := match marker {
        .circle => 1
        .cross => 2
        .none => {
            tmp := 4
            tmp2 := 6
            break 3
        }
    }

    match marker {
        .circle => {}
        else => {}
    }
}

