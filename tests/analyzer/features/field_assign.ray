struct Pet {
    name: str,
    has_eaten: bool,
}

struct Person {
    name: str,
    pet: Pet,
}

fn main() {
    pet := Pet{ name="Vodka", has_eaten=true }
    tom := Person{ name="Tom", pet }
    tom.name = "Martin"
    tom.pet.has_eaten = false
}
