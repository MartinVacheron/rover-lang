struct Human {
    age: int,
    name: str,

    fn new(age: int) -> Self {
        name := "Tom"
        return .{
            age,
            name,
        }
    }
}

answer := 42

fn main() {
    human := Human{ age=5, name="Tom" }
    human = Human{ name="Tom", age=5 }

    age := 95
    human = Human{ age, name="Tom" }

    name := "Tom"
    human = Human{ age=6, name }

    human = Human{ age, name }
    human = Human{ name, age }

    struct Local {
        answer: int
    }

    adr := Local{answer}
}
