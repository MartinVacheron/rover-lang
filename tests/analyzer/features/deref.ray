struct Bar {
    a: bool,
    b: str,
}

struct Foo {
    b: *int,
    c: *Bar,
    d: **int,
    e: **Bar,
}

fn main() {
    a := "sss"

    b := *a
    print b.*
 
    c := *b
    print c.*.*

    value := 9
    bar := Bar{ a=true, b="ok" }
    pvalue := *value
    pbar := *bar

    foo := Foo{ b=*value, c=*bar, d=*pvalue, e=*pbar }
    print foo.b.* == value
    print foo.c.*.a == bar.a
    print foo.d.*.* == value
    print foo.e.*.*.b == bar.b
}

